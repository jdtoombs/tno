import { IContentModel } from 'hooks';

export function formContentToApiContent(formValues: IContentModel): any {
  // return form values in valid API format on submit of ContentForm
  return formValues;
}
